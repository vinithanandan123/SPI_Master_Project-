# SPI_Master_Project-
SPI Master Controller using Verilog HDL is a digital hardware design project that implements an 8-bit Serial Peripheral Interface (SPI) Master for synchronous serial communication. The design generates the SPI clock (SCLK), controls the Chip Select (CS) signal, and transmits parallel 8-bit data serially through the Master Out Slave In (MOSI) line.
